# Install on every machine:  pip install -r requirements.txt
# torch is intentionally NOT listed: Kaggle/Colab ship a CUDA-matched build -- reinstalling it breaks the GPU.
# Local machine only:  pip install torch  (pick the CUDA build from pytorch.org)
# After your first successful run, freeze exact versions:  pip freeze > requirements.lock.txt
numpy
pandas
scipy
scikit-learn>=1.3      # StratifiedGroupKFold
lightgbm>=4.0
xgboost
catboost
polars
transformers
sentencepiece          # DeBERTa-v3 tokenizer
protobuf               # needed by the DeBERTa-v3 tokenizer conversion
accelerate
datasets
tqdm
matplotlib
seaborn
